<img
    data-slot="avatar-image"
    data-avatar-target="image"
    data-action="error->avatar#showFallback"
    class="{{ ('aspect-square size-full ' ~ attributes.render('class'))|tailwind_merge }}"
    {{ attributes.defaults({alt: ''}) }}
/>
