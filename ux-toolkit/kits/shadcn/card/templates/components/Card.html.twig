{# @block content The card content, typically includes `Card:Header`, `Card:Content`, and/or `Card:Footer` #}
<div
    data-slot="card"
    class="{{ ('bg-card text-card-foreground flex flex-col gap-6 rounded-xl border py-6 shadow-sm ' ~ attributes.render('class'))|tailwind_merge }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</div>
