{# @block content The main content area of the card #}
<div
    data-slot="card-content"
    class="{{ ('px-6 ' ~ attributes.render('class'))|tailwind_merge }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</div>
