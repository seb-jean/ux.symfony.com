{# @block content The keyboard shortcut combination, typically multiple `Kbd` components #}
<kbd
    data-slot="kbd-group"
    class="{{ ('gap-1 inline-flex items-center ' ~ attributes.render('class'))|tailwind_merge }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</kbd>
