{# @block content The default block #}
<p
    data-slot="text"
    id="{{ _dialog_description_id }}"
    class="{{ (attributes.render('class') ~ ' mt-2 text-pretty text-base/6 text-zinc-500 sm:text-sm/6 dark:text-zinc-400')|trim }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</p>
