{
    "$schema": "../../../schema-kit-recipe-v1.json",
    "type": "component",
    "name": "Description list",
    "description": "For when you need to take the data from just one table row and somehow turn it into its own entire table to keep the page from feeling too empty.",
    "copy-files": {
        "templates/": "templates/"
    }
}
