{# @block content The default block #}
<dl
    class="{{ (attributes.render('class') ~ ' grid grid-cols-1 text-base/6 sm:grid-cols-[min(50%,--spacing(80))_auto] sm:text-sm/6')|trim }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</dl>
