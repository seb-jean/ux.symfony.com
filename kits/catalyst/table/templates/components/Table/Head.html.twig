{# @block content The default block #}
<thead
    class="{{ (attributes.render('class') ~ ' text-zinc-500 dark:text-zinc-400')|trim }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</thead>
