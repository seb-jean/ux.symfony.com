{# @block content The default block #}
<p
    data-slot="text"
    class="{{ (attributes.render('class') ~ ' text-base/6 text-zinc-500 sm:text-sm/6 dark:text-zinc-400')|trim }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</p>
