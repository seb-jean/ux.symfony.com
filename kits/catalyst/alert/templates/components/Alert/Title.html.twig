{# @block content The default block #}
<h2
    id="{{ _alert_title_id }}"
    class="{{ (attributes.render('class') ~ ' text-center text-base/6 font-semibold text-balance text-zinc-950 sm:text-left sm:text-sm/6 sm:text-wrap dark:text-white')|trim }}"
    {{ attributes }}
>
    {%- block content %}{% endblock -%}
</h2>
