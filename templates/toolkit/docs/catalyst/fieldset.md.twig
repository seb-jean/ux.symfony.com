{% extends 'toolkit/docs/_base_component.md.twig' %}

{% block demo %}
{{ toolkit_code_demo(kit_id.value, component.name) }}
{% endblock %}

{% block examples %}
### Basic example
Use the `Fieldset`, `Legend`, `Text`, and `Field:Group` components to group a subset of form controls together:
{{ toolkit_code_example(kit_id.value, component.name, 'Basic example') }}

### Without legend
Use a `Fieldset` with `aria-label` to group a set of form controls together without a `Legend`:
{{ toolkit_code_example(kit_id.value, component.name, 'Without legend') }}

### Without role
Use the `Field:Group` component on its own to use it solely for layout, without adding `role="group"` and announcing it to assistive technology like a traditional `fieldset`:
{{ toolkit_code_example(kit_id.value, component.name, 'Without role') }}

### With grid layout
For more complex layouts like grids, use wrapper elements to create nested form control groups and style them yourself with utility classes:
{{ toolkit_code_example(kit_id.value, component.name, 'With grid layout') }}

### With custom layout
Use a plain `<div>` instead of a `Field:Group` along with the unstyled `Field` component from `@headlessui/react` to implement a fully custom layout:
{{ toolkit_code_example(kit_id.value, component.name, 'With custom layout') }}
Add `data-slot="control"` to a child of your `Fieldset` if you want it to receive the same layout styles as a `Field:Group`.

### Disabled state
Add the `disabled` prop to a `Fieldset` component to disable the entire fieldset:
{{ toolkit_code_example(kit_id.value, component.name, 'Disabled state') }}
{% endblock %}
