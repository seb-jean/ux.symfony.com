{% extends 'toolkit/docs/_base_component.md.twig' %}

{% block demo %}
{{ toolkit_code_demo(kit_id.value, component.name) }}
{% endblock %}

{% block examples %}
### Basic example
Use the `Checkbox` component on its own to render a standalone checkbox without an associated `Label` component:
{{ toolkit_code_example(kit_id.value, component.name, 'Basic example') }}
Make sure to provide an `aria-label` for assistive technology, or connect the Checkbox to your own `<label>` element using an `id`.

### With label
Wrap a `Label` and `Checkbox` with the `Checkbox:Field` component to automatically associate them using a generated ID:
{{ toolkit_code_example(kit_id.value, component.name, 'With label') }}

### With description
Use the `Checkbox:Field`, `Label`, and `Description` components to add a description below the checkbox:
{{ toolkit_code_example(kit_id.value, component.name, 'With description') }}

### With custom layout
Use the unstyled `Field` component from `@headlessui/react` directly instead of the styled `Checkbox:Field`:
{{ toolkit_code_example(kit_id.value, component.name, 'With custom layout') }}
Using the unstyled `Field` component will ensure important accessibility features are still handled for you like generating IDs and associating elements using `aria-*` attributes.

### With accent color
Use the `color` prop to choose a different accent color for a checkbox:
{{ toolkit_code_example(kit_id.value, component.name, 'With accent color') }}

### With custom value
Use the `value` prop to specify a custom value to use when submitting a form:
{{ toolkit_code_example(kit_id.value, component.name, 'With custom value') }}

### Default checked state
Use the `defaultChecked` prop to set the default state when using the `Checkbox` as an uncontrolled component:
{{ toolkit_code_example(kit_id.value, component.name, 'Default checked state') }}

### Indeterminate state
Use the `indeterminate` prop to indicate that the `Checkbox` is in an indeterminate state, neither toggled on nor off:
{{ toolkit_code_example(kit_id.value, component.name, 'Indeterminate state') }}

### Controlled component
Use the `checked` and `onChange` props to use a `Checkbox` as a controlled component:
{{ toolkit_code_example(kit_id.value, component.name, 'Controlled component') }}

### Multiple checkboxes
Use the `Checkbox:Group` component to stack multiple checkboxes together in a list:
{{ toolkit_code_example(kit_id.value, component.name, 'Multiple checkboxes') }}

### With fieldset
Use the `Fieldset`, `Legend`, and `Text` components to add a title and description to a group of checkboxes:
{{ toolkit_code_example(kit_id.value, component.name, 'With fieldset') }}
You can optionally add `role="group"` and an `aria-label` to the `Checkbox:Group` if all of the checkboxes are related and you want them announced to assistive technology as a group.

### Disabled state
Add the `disabled` prop to a `Checkbox` or `Checkbox:Field` component to disable it:
{{ toolkit_code_example(kit_id.value, component.name, 'Disabled state') }}
You can also add the `disabled` prop to a `Fieldset` to disable the entire fieldset.
{% endblock %}
