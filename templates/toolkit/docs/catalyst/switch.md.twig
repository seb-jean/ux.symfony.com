{% extends 'toolkit/docs/_base_component.md.twig' %}

{% block demo %}
{{ toolkit_code_demo(kit_id.value, component.name) }}
{% endblock %}

{% block examples %}
### Basic example
Use the `Switch` component on its own to render a standalone switch without an associated `Label` component:
{{ toolkit_code_example(kit_id.value, component.name, 'Basic example') }}
Make sure to provide an `aria-label` for assistive technology, or connect the `Switch` to your own `<label>` element using an `id`.

### With label
Wrap a `Label` and `Switch` with the `Switch:Field` component to automatically associate them using a generated ID:
{{ toolkit_code_example(kit_id.value, component.name, 'With label') }}

### With description
Use the `Switch:Field`, `Label`, and `Description` components to add a label and description to a switch:
{{ toolkit_code_example(kit_id.value, component.name, 'With description') }}

### With custom layout
Use the unstyled `Field` component from `@headlessui/react` directly instead of the styled `Switch:Field` component to implement a custom layout:
{{ toolkit_code_example(kit_id.value, component.name, 'With custom layout') }}
Using the unstyled components from Headless UI will ensure important accessibility features are still handled for you like generating IDs and associating elements using `aria-*` attributes.

### With accent color
Use the `color` prop to choose a different accent color for a switch:
{{ toolkit_code_example(kit_id.value, component.name, 'With accent color') }}

### With custom value
Use the `value` prop to specify a custom value to use when submitting a form:
{{ toolkit_code_example(kit_id.value, component.name, 'With custom value') }}

### Default checked state
Use the `defaultChecked` prop to set the default state when using the `Switch` as an uncontrolled component:
{{ toolkit_code_example(kit_id.value, component.name, 'Default checked state') }}

### Controlled component
Use the `checked` and `onChange` props to use a `Switch` as a controlled component:
{{ toolkit_code_example(kit_id.value, component.name, 'Controlled component') }}

### Multiple switches
Use the `Switch:Group` component to stack multiple switches together in a list:
{{ toolkit_code_example(kit_id.value, component.name, 'Multiple switches') }}
You can optionally add `role="group"` and an `aria-label` to the `Switch:Group` if all of the switches are related and you want them announced to assistive technology as a group.

### With fieldset
Use the `Fieldset`, `Legend`, and `Text` components to add a title and description to a group of switches:
{{ toolkit_code_example(kit_id.value, component.name, 'With fieldset') }}

### Disabled state
Add the `disabled` prop to a `Switch` or `Switch:Field` component to disable it:
{{ toolkit_code_example(kit_id.value, component.name, 'Disabled state') }}
{% endblock %}
